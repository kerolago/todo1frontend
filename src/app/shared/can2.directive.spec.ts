import { Can2Directive } from './can2.directive';

describe('Can2Directive', () => {
  it('should create an instance', () => {
    const directive = new Can2Directive();
    expect(directive).toBeTruthy();
  });
});
